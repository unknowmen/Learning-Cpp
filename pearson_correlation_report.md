# รายงานโปรแกรม Pearson Correlation สำหรับตัวแปรเชิงตัวเลข 2 ตัว

วันที่จัดทำ: 22 กันยายน 2026

## 1. วัตถุประสงค์

โปรแกรม `probability.c++` รับข้อมูลเชิงตัวเลขสองตัวแปร ได้แก่ `x` และ `y` ซึ่งข้อมูลแต่ละตำแหน่งต้องเป็นคู่สังเกตเดียวกัน จากนั้นคำนวณ Pearson correlation coefficient เพื่อวัดความสัมพันธ์เชิงเส้นระหว่างตัวแปรทั้งสอง

## 2. รูปแบบข้อมูลเข้า

ผู้ใช้ป้อนข้อมูลตามลำดับดังนี้:

1. จำนวนคู่ข้อมูล `n`
2. ค่า `x` จำนวน `n` ค่า
3. ค่า `y` จำนวน `n` ค่า

ตัวอย่าง:

```text
10
8 8 9 9 10 10 12 12 14 15
7 8 8 10 8 9 9 11 12 14
```

ข้อมูลจะถูกจับคู่ตามตำแหน่ง เช่น `(x[0], y[0])`, `(x[1], y[1])` และต่อไปจนครบ `n` คู่

## 3. ค่าที่โปรแกรมคำนวณ

โปรแกรมสะสมค่าต่อไปนี้จากข้อมูลทุกคู่:

- ผลรวมผลคูณ `sum_xiyi` = Σ(xᵢyᵢ)
- ผลรวม `sum_xi` = Σxᵢ
- ผลรวม `sum_yi` = Σyᵢ
- ผลรวมกำลังสอง `sum_xi_squared` = Σxᵢ²
- ผลรวมกำลังสอง `sum_yi_squared` = Σyᵢ²
- กำลังสองของผลรวม `squared_sum_x` = (Σxᵢ)²
- กำลังสองของผลรวม `squared_sum_y` = (Σyᵢ)²

ค่าทั้งห้ารายการแรกคำนวณภายใน loop เดียว เพื่อลดจำนวนครั้งที่โปรแกรมต้องเดินผ่านข้อมูล

## 4. สูตร Pearson correlation

โปรแกรมใช้สูตร:

```text
                  nΣ(xᵢyᵢ) - (Σxᵢ)(Σyᵢ)
r = -------------------------------------------------------
    sqrt([nΣxᵢ² - (Σxᵢ)²] [nΣyᵢ² - (Σyᵢ)²])
```

ค่า `r` มีช่วงทางทฤษฎีตั้งแต่ `-1` ถึง `1`:

- ใกล้ `1` หมายถึงความสัมพันธ์เชิงเส้นทางบวกสูง
- ใกล้ `-1` หมายถึงความสัมพันธ์เชิงเส้นทางลบสูง
- ใกล้ `0` หมายถึงไม่มีความสัมพันธ์เชิงเส้นที่ชัดเจน

Correlation ไม่ได้ยืนยันความเป็นเหตุและผลระหว่างตัวแปร

## 5. การแปลผล

โปรแกรมใช้ `abs(r)` เพื่อวัดความแรง จึงรองรับทั้งความสัมพันธ์ทางบวกและทางลบ:

| ช่วงของ `abs(r)` | ระดับ |
|---|---|
| 0.80–1.00 | Very Strong |
| 0.60–น้อยกว่า 0.80 | Strong |
| 0.40–น้อยกว่า 0.60 | Moderate |
| 0.20–น้อยกว่า 0.40 | Weak |
| 0.00–น้อยกว่า 0.20 | Very Weak |

ทิศทางแสดงแยกเป็น `Positive`, `Negative` หรือ `No linear correlation`

## 6. การตรวจสอบข้อผิดพลาด

โปรแกรมตรวจกรณีต่อไปนี้:

- จำนวนคู่ข้อมูลต้องไม่น้อยกว่า 2
- ถ้า `x` หรือ `y` มีค่าเหมือนกันทั้งหมด denominator จะเป็นศูนย์ และ Pearson correlation จะไม่มีนิยาม
- โปรแกรมจองพื้นที่ vector ล่วงหน้าด้วย `reserve()` ตามจำนวนข้อมูล เพื่อลดการขยายหน่วยความจำระหว่างรับข้อมูล

## 7. การวัดเวลา

โปรแกรมใช้ `std::chrono::steady_clock` จับเวลาเฉพาะขั้นตอนคำนวณ โดยเริ่มหลังรับข้อมูลครบแล้วและหยุดก่อนแสดงผล จึงไม่นับเวลาที่ผู้ใช้พิมพ์หรือเวลาที่ terminal ใช้พิมพ์ข้อความ

ผลลัพธ์แสดงเป็น nanoseconds (`ns`) ตัวเลขอาจเปลี่ยนในแต่ละครั้งตามภาระงานของระบบ CPU โหมดการ compile และขนาดข้อมูล สำหรับข้อมูลจำนวนน้อย ค่าที่วัดได้มีความผันผวนสูง

## 8. ประสิทธิภาพ

- เวลาในการคำนวณเพิ่มตามจำนวนคู่ข้อมูล: `O(n)`
- หน่วยความจำสำหรับเก็บ `x` และ `y`: `O(n)`
- การสะสมค่าทางสถิติใช้ loop เดียวผ่านข้อมูลทุกคู่

สำหรับการวัดประสิทธิภาพจริงควร compile แบบ optimized:

```bash
g++ -std=c++20 -O2 -Wall -Wextra -pedantic probability.c++ -o probability
```

## 9. ผลการทดสอบ

ชุดข้อมูลตัวอย่าง 10 คู่ให้ผล:

```text
Pearson r : 0.893639
Strength : Very Strong
Direction : Positive
```

ชุดข้อมูล `x = {1, 2, 3}` และ `y = {3, 2, 1}` ให้ผล:

```text
Pearson r : -1
Strength : Very Strong
Direction : Negative
```

กรณีตัวแปรใดตัวแปรหนึ่งมีค่าเหมือนกันทั้งหมด โปรแกรมจะแสดง:

```text
Pearson correlation is undefined
```

## 10. สรุป

โปรแกรมสามารถคำนวณ Pearson correlation สำหรับตัวแปรเชิงตัวเลขสองตัว แสดงค่าส่วนประกอบของสูตร แปลระดับความแรงและทิศทาง ตรวจกรณีที่คำนวณไม่ได้ และรายงานเวลาที่ใช้เฉพาะส่วนคำนวณได้ครบถ้วน
